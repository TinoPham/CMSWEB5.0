@model SVRDatabase.Model.ServiceConfig

@{
    ViewBag.Title = "Edit";
    //Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ServiceConfig</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ID)

        <div class="form-group">
            <label class="control-label col-md-2">Converter Limitted</label>
            <div class="col-md-3">
                @Html.EditorFor(model => model.ConverterLimit)
                @Html.ValidationMessageFor(model => model.ConverterLimit)
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">DVR Authenticate</label>
            <div class="col-md-3">
                @Html.EditorFor(model => model.DVRAuthenticate)
                @Html.ValidationMessageFor(model => model.DVRAuthenticate)
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">Converter Interval</label>
            <div class="col-md-3">
                @Html.EditorFor(model => model.ConverterInterval)
                @Html.ValidationMessageFor(model => model.ConverterInterval)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn btn-success" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
